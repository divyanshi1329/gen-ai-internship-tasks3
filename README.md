task3_req_to_spec_tool (Task 3)

Tool: Requirement → Modules → Schema → APIs → Pseudocode → exports (JSON/Markdown/PDF)
📁 Outputs: output/spec.json, output/spec.md, output/spec.pdf + report included
✅ Prototype is built as a Streamlit application for easy demo.
## Setup
1. Clone the repo
2. Create `.env` file using `.env.example`
3. Install requirements:
   pip install -r requirements.txt
4. Run:
   python main.py
